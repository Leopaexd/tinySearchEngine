package tinySearchEngine;

class queryParser {
	//Parses search queries as described på the context free grammar provided in instructions for project 1
	
	static String[] parse (String query){
		String[] parsedQuery = query.split(" ");
		
		
		
		
		return parsedQuery;
	}
}
